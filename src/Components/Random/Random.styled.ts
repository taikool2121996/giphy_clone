import styled from 'styled-components';

export const RandomWrapper = styled.article`
  padding: 2rem;
  background-color: ${(props) => props.theme.colorBg2};
  border-radius: 1rem;
  width: 50%;
  margin: 0 auto;
`;
